xvfb-run FreeFem++ exam.edp

for f in *.eps; do convert "$f" "${f%.eps}.png" && rm "$f"; done


for f in *.eps; do
    convert -density 1200 "$f" -quality 100 "${f%.eps}.png" && rm "$f"
done

nanoparticule_substrat_newton

git status
git add .
git commit -m "rr"
git push